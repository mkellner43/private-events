<nav class="navbar navbar-expand-lg bg-dark navbar-dark">
  <div class="container-fluid">
    <%= link_to "Private Events", root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Create Event", new_event_path, class:"nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to "Your events", show_user_path(current_user), class:"nav-link active" %>
          </li>
        <% end %>
      </ul>
    <% if user_signed_in? %>
      <%= link_to "Sign out", destroy_user_session_path, data: {turbo_method: :destroy, turbo_confirm: "Are you sure you want to log out?"}, class: "btn btn-secondary" %>
    <% else %>
        <%= link_to "Log In", new_user_session_path, class:"btn btn-secondary me-2" %>
    <%= link_to "Sign Up", new_user_registration_path, class:"btn btn-secondary me-2" %>
    <% end %>
    </div>
  </div>
</nav>